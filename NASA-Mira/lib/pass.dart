// Flutter
import "package:flutter/material.dart";

// TODO(KristofKekesi): Replace with api key before release.
/// NASA API key.
const String apiKey = "DEMO_KEY";

/// A [String] to store the app's name.
const String appTitle = "NASA Mira";
/// A [String] to store the path to the app's logo.
const String appLogo = "assets/nasa-logo.png";
/// A [String] to store the path to the app's background.
const String appBackground = "assets/nasa-background.jpg";
/// A [Color] to store the Color of the app.
const Color appColor = Color(0xffE8672D);
